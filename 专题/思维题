1、Intersection(zoj 3862)
题意:
    给你2*n个点坐标，其中两两相连，但不存在三点成线和共点，有n条边，求能否在n+10操作步数内完成点交换，使得边没有相交。
思路：
    按照贪心思想，将点进行排列，比如按x从小到大进行排列，如果x相等则按y从小到大进行排列，这样从[1,2n]逐个取点肯定不会有相交。
关键是交换点的操作，设link[i]=j表示(i,j)两点相连，设pos[p[i].id]=i表示排序后p[i].id所在的位置为排序后数组位置i。之后如果
遍历到的两个点没有关系，则罗列4个id位置a,b,c,d(a-c,b-d)，因为b,c需要交换，此时因为是交换点操作，我们只需要先把点id进行交换
swap(p[pos[b]].id,p[pos[c]].id); 然后交换pos的映射swap(pos[b],pos[c]); 最后输出即可得到答案。

反思：
    对于没有思路可能涉及算法的题目，如若不行，就努力向思维考虑。有时候通过贪心就可以得到答案。
